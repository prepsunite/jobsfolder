{
  "name": "jobsfolder-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "ci:check": "npm run ci:frontend",
    "ci:frontend": "cd frontend && npm run lint && npx tsc --noEmit && npm run build"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.111.0",
    "razorpay": "^2.9.8"
  }
}
